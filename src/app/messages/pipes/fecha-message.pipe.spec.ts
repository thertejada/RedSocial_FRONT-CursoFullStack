import { FechaMessagePipe } from './fecha-message.pipe';

describe('FechaMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
