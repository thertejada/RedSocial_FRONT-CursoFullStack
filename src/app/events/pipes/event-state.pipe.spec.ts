import { EventStatePipe } from './event-state.pipe';

describe('EventStatePipe', () => {
  it('create an instance', () => {
    const pipe = new EventStatePipe();
    expect(pipe).toBeTruthy();
  });
});
